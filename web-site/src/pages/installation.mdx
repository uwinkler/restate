---
title: Installation
path: /installation/
---

import { MdxLayout } from "../layouts/mdx-layout"
import { Link } from "gatsby"
import { InfoPanel, WarningPanel } from "../components/panel"
export default MdxLayout

With NPM:

```code
npm install @restate/core --save
```

or with YARN:

```code
yarn add @restate/core
```

If you want, you can also instal the <Link to="/quick-dev-tools">ReState-DevTools</Link>
