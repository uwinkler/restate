{
  "dependencies": {
    "lerna": "^3.13.2"
  },
  "scripts": {
    "publish": "lerna run clean && lerna run build && lerna run test && git commit -am 'PUBLISH' && lerna publish"
  },
  "name": "rx-state",
  "license": "MIT",
  "version": "0.0.0"
}
